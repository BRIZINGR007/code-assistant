{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Gin Server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server", // <- folder not file!
            "env": {
                "GIN_MODE": "debug",
                "PORT": ""
            },
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "showLog": true
        }
    ]
}